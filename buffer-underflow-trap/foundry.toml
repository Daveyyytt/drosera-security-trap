[profile.default]
src = "src"
out = "out"
libs = ["lib"]

# Enable optimizer for gas efficiency
optimizer = true
optimizer_runs = 200

# Hoodi Testnet configuration
[rpc_endpoints]
hoodi = "https://rpc.hoodi.ethpandaops.io/"

[etherscan]
hoodi = { key = "" }
